# eslint-import-resolver-custom-alias declares peer eslint-plugin-import@>=2.2; npm still pulls eslint-plugin-import@2.x
# for that resolver even though we use eslint-plugin-import-x at runtime. Use legacy peer resolution until the resolver supports import-x.
legacy-peer-deps=true
